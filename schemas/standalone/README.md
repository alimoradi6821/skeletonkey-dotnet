# Standalone execution settings schemas

`0.1/schema.json` describes the structural JSON contract for SkeletonKey Standalone Export execution settings.

The C# parser remains the semantic authority for constraints that JSON Schema does not fully express here, including the bounded ISO-8601 interval range, duplicate-property rejection, and local-time scheduling behavior.
