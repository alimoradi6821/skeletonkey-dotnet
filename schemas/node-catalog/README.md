# Node Catalog Schemas

Node catalog schemas define the JSON artifact shape for contract-only node catalogs.

- `0.1/schema.json` is the current pre-release node catalog schema.

Schema validation checks JSON shape. Semantic validation checks node type identity, duplicate exact definitions, port direction consistency, dynamic port rules, capability IDs, resource slots, and deprecation consistency.
