{
  "$schema": "https://schemas.skeletonkey.dev/workflow/0.1/schema.json",
  "specVersion": "0.1.0",
  "id": "interaction-secret-with-default",
  "name": "Interaction Secret With Default",
  "nodes": [
    { "id": "start", "type": "core.start", "typeVersion": 1 },
    { "id": "request", "type": "interaction.request", "typeVersion": 1, "parameters": { "kind": "secret", "prompt": "Secret.", "default": "secret" } }
  ],
  "connections": [
    { "from": { "node": "start", "port": "main" }, "to": { "node": "request", "port": "main" } }
  ]
}
