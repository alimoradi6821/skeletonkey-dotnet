{
  "$schema": "https://schemas.skeletonkey.dev/workflow/0.1/schema.json",
  "specVersion": "0.1.0",
  "id": "binding-invalid-default-policy",
  "name": "Binding invalid default policy",
  "inputs": { "account": { "type": "object", "required": false } },
  "variables": {},
  "nodes": [
    { "id": "start", "type": "core.start", "typeVersion": 1, "disabled": false, "parameters": {} },
    { "id": "invoke", "type": "workflow.invoke", "typeVersion": 1, "disabled": false, "parameters": { "workflow": { "id": "child-workflow" }, "inputs": { "account": { "$binding": { "source": "input", "name": "account", "default": "fallback" } } } } }
  ],
  "connections": [
    { "from": { "node": "start", "port": "main" }, "to": { "node": "invoke", "port": "main" } }
  ],
  "outputs": {}
}
